# AWS S3 Configuration for Image Uploads
# Create an S3 bucket and add these credentials to your .env.local file

AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
AWS_S3_BUCKET=your_bucket_name_here

# Make sure your S3 bucket has the following configuration:
# 1. Block public access is OFF (or configure for public read access)
# 2. Bucket policy allows public read access to objects
# 3. CORS configuration allows your domain

# Example S3 Bucket Policy:
# {
#   "Version": "2012-10-17",
#   "Statement": [
#     {
#       "Sid": "PublicReadGetObject",
#       "Effect": "Allow",
#       "Principal": "*",
#       "Action": "s3:GetObject",
#       "Resource": "arn:aws:s3:::your-bucket-name/*"
#     }
#   ]
# }

# Example CORS Configuration:
# [
#   {
#     "AllowedHeaders": ["*"],
#     "AllowedMethods": ["GET", "PUT", "POST"],
#     "AllowedOrigins": ["http://localhost:3000", "https://yourdomain.com"],
#     "ExposeHeaders": []
#   }
# ]
